# Experiments

`experiments/`는 정식 구조로 편입하기 전의 비교 실험, PoC, 임시 검증을 담는 영역이다.

예시:

- 모델 응답 품질 비교
- Tool calling 방식 비교
- LangChain vs Direct Agent 비교
- MCP transport 비교
- local runtime 성능 측정

실험이 안정화되면 `platform/`, `tools/`, `agents/`, `mcp/` 중 적절한 영역으로 승격한다.

